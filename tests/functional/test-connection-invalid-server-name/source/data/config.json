{
  "parameters": {
    "db": {
      "host": "${DB_HOST}",
      "serverName": "invalid_server_name",
      "port": "${DB_PORT}",
      "database": "${DB_DATABASE}",
      "user": "${DB_USER}",
      "#password": "${DB_PASSWORD}"
    }
  },
  "action": "testConnection"
}
