{
  "parameters": {
    "db": {
      "host": "${DB_HOST}",
      "serverName": "${DB_SERVER_NAME}",
      "port": "${DB_PORT}",
      "database": "${DB_DATABASE}",
      "user": "${DB_USER}",
      "#password": "${DB_PASSWORD}"
    }
  },
  "action": "testConnection"
}
