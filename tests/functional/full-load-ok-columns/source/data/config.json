{
  "parameters": {
    "db": {
      "host": "${DB_HOST}",
      "serverName": "${DB_SERVER_NAME}",
      "port": "${DB_PORT}",
      "database": "${DB_DATABASE}",
      "user": "${DB_USER}",
      "#password": "${DB_PASSWORD}"
    },
    "id": 123,
    "name": "config-row-1",
    "outputTable": "output",
    "table": {
      "tableName": "simple",
      "schema": "${DEFAULT_SCHEMA}"
    },
    "columns": ["id", "date"],
    "retries": 2
  }
}
